error: #[input] takes a struct with named fields, and this is a tuple struct. `#[input]` bundles `validator::Validate`, whose derive supports only that shape (`Serialize`, `Deserialize` and `JsonSchema` do not mind). For a tagged union, put `#[input]` on each variant's payload struct and derive the wire traits on the enum itself; for a newtype, give the field a name.
 --> tests/integration/diagnostics/input_on_a_tuple_struct.rs:8:1
  |
8 | pub struct Slug(pub String);
  | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
