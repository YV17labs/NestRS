//! Integration tests mirroring `src/` (see CLAUDE.md) — one binary, one module per concern.

mod body_limit;
mod compression;
mod diagnostics;
mod exclusive_paths;
mod global_prefix;
mod route_decorators;
