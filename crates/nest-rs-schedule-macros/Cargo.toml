[package]
name = "nest-rs-schedule-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Internal — the #[scheduled] decorator macro; re-exported by nest-rs-schedule, not a direct dependency."

[lib]
proc-macro = true

[dependencies]
syn.workspace = true
quote.workspace = true
proc-macro2.workspace = true
# Validate a `cron(...)` string literal at macro-expansion time, so a bad
# expression is a compile error rather than a boot-time surprise.
croner.workspace = true
