---
title: NestRS — multi-tenant isolation you cannot forget
description: A Rust backend framework where row-level filtering, masking, and transactions are structural — transparent, non-optional, verified at boot.
template: splash
head:
  - tag: meta
    attrs:
      property: og:type
      content: website
hero:
  title: |
    <span class="wordmark"><span class="wm-nest">Nest</span><span class="wm-rs">RS</span></span>
  tagline: |
    <span class="hero-lead">Multi-tenant isolation you <span class="lead-accent">cannot forget.</span></span>
    <span class="hero-sub">Row-level filtering, per-field masking, and transactions — transparent, non-optional, verified at boot. You write a controller, a service, a module; decorators carry the security and the wiring. Native speed comes free: <strong>~25×</strong> Node's hello-world throughput in <strong>4–6 MB</strong> of RAM — the full auth + row-level + masking pipeline, Postgres included, holds <strong>~23k req/s</strong> at <strong>p99 &lt; 4.5 ms</strong> in <strong>~32 MB</strong>. Both measured below. Familiar if you come from NestJS, Spring, or Laravel.</span>
    <span class="hero-stack">
      <span>HTTP</span><span>OpenAPI</span><span>GraphQL</span><span>WebSockets</span><span>MCP</span>
      <span>Database</span><span>Queue</span><span>Schedule</span><span>OpenTelemetry</span>
      <span class="hero-stack-accent">Authentication</span><span class="hero-stack-accent">OAuth2</span><span class="hero-stack-accent">Authorization</span>
    </span>
  image:
    alt: NestRS logo
    file: ../../assets/logo.svg
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
      variant: primary
    - text: Why NestRS
      link: /why/
      variant: minimal
    - text: View on GitHub
      link: https://github.com/YV17labs/NestRS
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import Bench from '../../components/Bench.astro';
import HelloCodeTabs from '../../components/HelloCodeTabs.astro';
import TerminalSession from '../../components/TerminalSession.astro';

## The part you cannot forget

<div class="nestrs-reveal not-content">

This CRUD resource is authenticated, tenant-scoped, transactional, and
field-masked. The `impl` block is empty on purpose: the two guards declare the
posture, `#[crud]` generates the five routes, and one ability does the rest.

```rust title="crates/features/src/orgs/http/controller.rs (abridged)"
#[controller(path = "/orgs")]
#[use_guards(AuthnGuard, AuthzGuard)]
pub struct OrgsController {
    #[inject]
    svc: Arc<OrgsService>,
}

#[crud(service = svc, entity = OrgEntity, output = Org,
       create = CreateOrg, update = UpdateOrg)]
impl OrgsController {}
```

The policy lives in one place. A member sees only their own org's users, and
only the fields the ability grants:

```rust title="crates/features/src/authz/ability.rs (abridged)"
ab.can(Action::Read, user::Entity)
    .when(|p| p.eq(user::Column::OrgId, actor.org_id))
    .fields([user::Column::Id, user::Column::Name]);
```

Same route, same handler — the caller's token decides the rows and the fields:

<TerminalSession
  label="Two tokens, one route"
  lines={[
    { kind: 'input', text: 'curl :3002/users -H "authorization: Bearer $ADMIN"   # admin' },
    { kind: 'response', text: '[{ "id": "…ac00", "name": "Acme Admin", "email": "admin@acme.test", … }, …]' },
    { kind: 'input', text: 'curl :3002/users -H "authorization: Bearer $MEMBER"  # plain member' },
    { kind: 'response', text: '[{ "id": "…ac00", "name": "Acme Admin" }, …]   # email masked, org-scoped' },
    { kind: 'input', text: 'curl -o /dev/null -w "%{http_code}" :3002/users/<globex-id> -H "…$MEMBER"' },
    { kind: 'response', text: '403   # cross-tenant read refused, decided by the framework' },
  ]}
/>

Posture is mandatory, not a convention: a GraphQL operation with neither
`#[authorize]` nor `#[public]` does not compile, and an HTTP controller whose
guard wiring is missing fails at boot — an ungated, unmasked surface cannot
ship.

Behind the guards sits a full authentication stack — JWT on EdDSA keys, OAuth2
with PKCE, Argon2id passwords with timing-safe verification, social login —
each a module import, documented in [Security](/security/).

**[See it live — the security checkpoint →](/tutorial/checkpoint/)**

</div>

## A complete service, one app

<div class="nestrs-reveal not-content">

No wiring file. No config. No boilerplate. And if Rust's learning curve is
what holds you back: the hard parts of framework Rust — lifetimes in
middleware, trait gymnastics, generic bounds — live inside the framework.
What's left in your files is structs, decorators, and async fns. Scaffold a **standalone** crate with
the CLI, then follow the tabs from **Controller** (your HTTP surface) through
**Service**, **Module**, and **Main** — that's a real, type-checked service.
The dependency graph is built and **verified at boot**, and the crate compiles
to a native binary. Start here; grow into a **workspace** when you add more apps
— same decorators, shared `crates/features/`.

<div class="nestrs-hello-demo">

<TerminalSession
  variant="compact"
  label="Scaffold"
  lines={[
    { kind: 'input', text: 'nestrs new hello --standalone' },
    { kind: 'output', text: 'Created standalone nestrs app at ./hello' },
    { kind: 'output', text: 'Template: hello — Hello World on GET /' },
    { kind: 'output', text: '  + hello/Cargo.toml' },
    { kind: 'output', text: '  + hello/rust-toolchain.toml' },
    { kind: 'output', text: '  + hello/.gitignore' },
    { kind: 'omit' },
    { kind: 'output', text: '  + hello/src/controller.rs' },
    { kind: 'output', text: '  + hello/tests/e2e/main.rs' },
    { kind: 'output', text: '' },
    { kind: 'output', text: 'Mode: standalone (one crate, logic in src/)' },
    { kind: 'output', text: '' },
    { kind: 'output', text: 'Next steps:' },
    { kind: 'output', text: '  cd ./hello' },
    { kind: 'output', text: '  nestrs run dev' },
    { kind: 'output', text: '  Open http://localhost:3000/ in your browser' },
  ]}
/>

<HelloCodeTabs />

<TerminalSession
  label="Run"
  lines={[
    { kind: 'input', text: 'nestrs run dev' },
    { kind: 'output', text: '    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.17s' },
    { kind: 'output', text: '     Running `target/debug/hello`' },
    { kind: 'output', text: '2026-04-29T12:00:00.799422Z  INFO nest_rs::module: module dependencies initialized module="HelloModule"' },
    { kind: 'output', text: '2026-04-29T12:00:00.799862Z  INFO nest_rs::routes: GET    /  (hello)' },
    { kind: 'omit' },
    { kind: 'output', text: '2026-04-29T12:00:00.802100Z  INFO poem::server: listening addr=socket://0.0.0.0:3000' },
    { kind: 'output', text: '2026-04-29T12:00:00.802552Z  INFO poem::server: server started' },
    { kind: 'output', text: '2026-04-29T12:00:00.803011Z  INFO nest_rs::access: method=GET path=/ status=200 bytes=11 duration_ms=0.189 client_ip=127.0.0.1 user_agent=curl/8.7.1 trace_id=48643283b6e9c222dadc23480117d76a' },
    { kind: 'input', text: 'curl http://localhost:3000' },
    { kind: 'response', text: 'Hello World' },
  ]}
/>

<p class="nestrs-firstbuild-note">As with any Rust dependency, the first <code>nestrs run dev</code> compiles the crate graph once. Every run after is the sub-second incremental rebuild shown above.</p>

</div>

The same inject-and-decorate model carries every surface — HTTP, GraphQL,
WebSockets, queues, scheduled jobs, MCP.
**[Get started →](/getting-started/)** · **[Why NestRS →](/why/)**

</div>

## How fast, exactly

The same "Hello World" service — a provider, a controller, a module — built
once in NestRS and once in NestJS 11 on Node.js 24, under identical `wrk` load
(each server pinned to 2 cores, `-t2 -c64 -d20s`). Synthetic baseline: same
handler shape and hardware — it isolates framework overhead, not a
domain-heavy API.

Two different measurements, don't conflate them: the **~23k req/s in ~32 MB**
in the hero is the demo API's **full pipeline** — JWT verification, ability
build, a row-level-filtered Postgres query, response masking. The
**~463k req/s and 4–6 MB** below are the **bare hello-world** — no database,
no auth. The gap between the two is the cost of real work (crypto + Postgres),
not of the framework.

Both ran on the same machine: a Linux Docker container capped at **4 cores
and 8 GB of RAM**, with the load generator competing for those same cores.
That setup understates the framework — on dedicated hardware, with the
client off-box, every figure on this page goes up. Measure it on yours.

If your point of comparison is axum or Actix rather than Node: same native
core, so the question is not speed — it is what the framework carries for you.
See [Why not axum?](/why-not-axum/).

**Read it the other way around: the fully-secured API still lands in the
range Node reaches serving a bare hello-world.**

<details>
<summary>How these numbers were taken</summary>

Both services ran on the same machine, inside a Linux Docker container
capped at 4 cores and 8 GB of RAM, each server pinned to 2 cores, with the
load generator sharing the remaining cores:

```bash
wrk -t2 -c64 -d20s http://localhost:3000/
```

The NestRS side is the scaffolded hello (`nestrs new hello --standalone`,
built in release); the Node side is the equivalent NestJS 11 hello-world on
Node.js 24 in production mode. The full-pipeline figures come from the same
container driving the seeded Publish `api` app's `GET /users` with a member
JWT — verification, ability build, row-level-filtered Postgres query, and
response masking on every request. The client competing for the same cores
understates both sides — measure on your own hardware.

</details>

<Bench />

## Why developers pick NestRS

<div class="nestrs-reveal nestrs-reveal--stagger not-content">

<CardGrid>
  <Card title="Secure & transactional by composition" icon="approve-check-circle">
    Authn, authorization, row-level filtering, response masking, transaction
    scope — you turn them on by importing a module, never by remembering to
    call them. Forgetting to redact a field becomes structurally hard.
  </Card>
  <Card title="Wiring you find out about at boot, not at 3am" icon="warning">
    The DI graph is checked at boot — a bad import fails startup with a
    clear error naming the missing wire. No reflection, no runtime
    "cannot resolve dependency" five minutes after deploy. The check lives
    in [`access.rs`](https://github.com/YV17labs/NestRS/blob/main/crates/nest-rs-core/src/access.rs).
  </Card>
  <Card title="Declarative, decorator-driven" icon="puzzle">
    `#[module]`, `#[controller]`, `#[resolver]`, `#[processor]`,
    `#[gateway]`, `#[scheduled]`, `#[mcp]`. You write logic; the decorator
    expands the boilerplate — into plain Rust you can read with
    [`cargo expand`](/why-not-axum/#the-macro-question). No service locator,
    no registration list to keep in sync.
  </Card>
  <Card title="Types you don't fight" icon="approve-check">
    Rust types end to end — entity, DTO, route handler, dataloader, GraphQL
    schema, OpenAPI doc. No `any`, no `unknown`, no `as` casts at the
    boundaries, no runtime "undefined is not a function."
  </Card>
  <Card title="Native throughput" icon="rocket">
    **~463k req/s** on 2 cores, **~231k per core**, sub-millisecond p99,
    no GC pauses (hello-world baseline — conditions above). The same
    `hyper`/`tokio` core as the fastest Rust web frameworks — with a
    structure on top.
  </Card>
  <Card title="An order of magnitude less RAM" icon="laptop">
    **~4 MB idle, ~6 MB under load** for the hello-world baseline — versus
    ~80–120 MB for an equivalent Node service; the full demo pipeline,
    Postgres included, stays around ~32 MB. Smaller instances, higher
    density, a materially lighter cloud bill.
  </Card>
  <Card title="Boots in milliseconds" icon="bars">
    Each deployable is a static native binary with no runtime to warm up —
    autoscaling and cold starts stop hurting.
  </Card>
  <Card title="Batteries included, opt-in by crate" icon="setting">
    HTTP, GraphQL, OpenAPI, WebSockets, Redis-backed queues, scheduling,
    an event bus, MCP, CASL-style authorization, health probes,
    OpenTelemetry — each an opt-in crate, so a worker compiles no HTTP
    stack at all. See the full list on [Packages](/packages/).
  </Card>
</CardGrid>

Every capability above ships today, each with a reference section on this
site.

</div>

## One domain, several workloads

**Microservice scaling without the distributed-systems tax.** A **workload**
is one deployable binary with one operational job — HTTP API, WebSocket
gateway, queue worker, MCP server. **Entities, services, and policy are
written once** in a shared `features` crate; each app under `apps/*` is a
thin composition root importing only the transports it serves, so a worker
never compiles the HTTP stack. One monorepo or several repositories —
coupling stays loose either way: self-contained tokens and a shared
database, no chatty RPC and no service mesh to get started.

Who runs where is a composition choice in each app's `module.rs`, not a
fork of entities and services. See [Getting started](/getting-started/)
and the [CLI](/cli/) to scaffold the workspace layout — schematically:

<div class="mono nestrs-reveal not-content">
  <div class="mono-deploy">
    <div class="mono-tier mono-tier--public">
      <header class="mono-tier-head">
        <span class="mono-tier-label">Public</span>
        <span class="mono-tier-hint">exposed to clients and partners</span>
      </header>
      <ul class="mono-apps mono-apps--pair">
        <li class="mono-app">
          <span class="mono-app-name">App A</span>
          <span class="mono-app-port">:3000</span>
          <span class="mono-app-role">a public surface</span>
        </li>
        <li class="mono-app">
          <span class="mono-app-name">App B</span>
          <span class="mono-app-port">:3001</span>
          <span class="mono-app-role">another public surface</span>
        </li>
      </ul>
    </div>

    <div class="mono-shared-mid">
      <span class="mono-shared-mid-label">shared features</span>
      <span class="mono-shared-mid-sub">one model — every app above and below builds on it</span>
    </div>

    <div class="mono-tier mono-tier--private">
      <header class="mono-tier-head">
        <span class="mono-tier-label">Private</span>
        <span class="mono-tier-hint">non-public · off the client request path</span>
      </header>
      <ul class="mono-apps mono-apps--pair">
        <li class="mono-app">
          <span class="mono-app-name">App C</span>
          <span class="mono-app-role">background work</span>
        </li>
        <li class="mono-app">
          <span class="mono-app-name">App D</span>
          <span class="mono-app-role">more background work</span>
        </li>
      </ul>
    </div>
  </div>
  <p class="mono-note">Same domain, shared features — one monorepo or several; which app runs where is an operational choice, not a rewrite of business logic.</p>
</div>

## Scale each workload on its own signal

In production you **scale horizontally per app** — the same binary, more
replicas when *its* signal moves: a hot public surface follows **demand**,
background apps follow **queue depth**. Each app under `apps/*` gets its
own replica count — no single "scale the monolith" knob.

<div class="scale nestrs-reveal not-content">
  <div class="scale-lanes scale-lanes--four">
    <article class="scale-lane scale-lane--api">
      <div class="scale-lane-top">
        <span class="scale-app">App A</span>
        <span class="scale-port">:3000</span>
        <span class="scale-signal">demand · high traffic</span>
        <span class="scale-arrow" aria-hidden="true">→</span>
        <span class="scale-replicas">×5</span>
      </div>
      <div class="scale-pills" aria-hidden="true">
        <span></span><span></span><span></span><span></span><span></span>
      </div>
    </article>
    <article class="scale-lane scale-lane--api scale-lane--steady">
      <div class="scale-lane-top">
        <span class="scale-app">App B</span>
        <span class="scale-port">:3001</span>
        <span class="scale-signal">demand · steady</span>
        <span class="scale-arrow" aria-hidden="true">→</span>
        <span class="scale-replicas">×2</span>
      </div>
      <div class="scale-pills scale-pills--compact" aria-hidden="true">
        <span></span><span></span>
      </div>
    </article>
    <article class="scale-lane scale-lane--worker">
      <div class="scale-lane-top">
        <span class="scale-app">App C</span>
        <span class="scale-signal">queue · backlog</span>
        <span class="scale-arrow" aria-hidden="true">→</span>
        <span class="scale-replicas">×4</span>
      </div>
      <div class="scale-pills scale-pills--worker" aria-hidden="true">
        <span></span><span></span><span></span><span></span>
      </div>
    </article>
    <article class="scale-lane scale-lane--worker scale-lane--light">
      <div class="scale-lane-top">
        <span class="scale-app">App D</span>
        <span class="scale-signal">queue · light</span>
        <span class="scale-arrow" aria-hidden="true">→</span>
        <span class="scale-replicas">×2</span>
      </div>
      <div class="scale-pills scale-pills--worker scale-pills--compact" aria-hidden="true">
        <span></span><span></span>
      </div>
    </article>
  </div>
  <p class="scale-aside">One product, four apps — A scales on traffic spikes, B stays lean, C follows backlog, D only when needed.</p>
</div>

## Going further

- [Getting started](/getting-started/) — scaffold and run your first app.
- [Tutorial](/tutorial/) — build a posts feature end to end.
- [Why NestRS](/why/) — the thesis and the structural properties behind it.
- [Threat model](/security/threat-model/) — the boundary each layer enforces,
  and how it composes with your deployment's own controls.
- [The Publish workspace](/publish/) — the product universe every example is drawn from.
