---
title: NestRS — The Rust framework for modular, scalable backends
description: "The Rust backend framework with NestJS-style modules and decorators: authn, tenant isolation, masking and transactions verified at boot, on a native core."
template: splash
head:
  - tag: meta
    attrs:
      property: og:type
      content: website
hero:
  title: |
    The Rust framework for <span class="accent">modular, scalable</span> backends.
  tagline: |
    You write the business logic; NestRS carries authentication, tenant isolation, field masking and transactions — declared as decorators, <strong>verified at boot</strong>.
  actions:
    - text: Get started
      link: /getting-started/
      variant: primary
    - text: Why NestRS
      link: /why/
      variant: minimal
    - text: cargo add nest-rs --features http,seaorm ↗
      link: https://crates.io/search?q=nest-rs
      variant: minimal
      attrs:
        class: rs-chip
---

import Band from '../../components/Band.astro';
import Bench from '../../components/Bench.astro';
import Capabilities from '../../components/Capabilities.astro';
import CodeCard from '../../components/CodeCard.astro';
import Guarantees from '../../components/Guarantees.astro';
import Workspace from '../../components/Workspace.astro';

<Band label="01 — SECURITY" alt>
<div class="rs-split">
<div>

## The part you cannot forget

A controller that looks like any controller — the security is in what you
*don't* write. Two declarations, and every route is authenticated, tenant-scoped
and field-masked.

<div class="rs-steps">

**Two guards on the struct.** Every route below requires a valid token and an
ability — forgetting one is a boot error, not a 3am incident.

**The handler is plain Rust.** No security code inside — rows are filtered and
fields masked on the way out, automatically.

**Policy lives in one place.** Which rows, which fields, for whom — one ability
rule, applied to every transport.

</div>

<a class="rs-more" href="/tutorial/checkpoint/">See it live — the security checkpoint →</a>

</div>
<div class="rs-evidence">

<CodeCard
  file="users/http/controller.rs"
  note="a normal controller"
  lines={[
    { code: '#[controller(path = "/users")]' },
    { code: '#[use_guards(AuthnGuard, AuthzGuard)]', mark: true, marker: 1 },
    { code: 'pub struct UsersController {' },
    { code: '    #[inject] svc: Arc<UsersService>,' },
    { code: '}' },
    { code: '' },
    { code: '#[routes]' },
    { code: 'impl UsersController {' },
    { code: '    #[get("/")]' },
    { code: '    async fn list(&self) -> Result<Vec<User>> {' },
    { code: '        self.svc.list().await', mark: true, marker: 2 },
    { code: '    }' },
    { code: '}' },
  ]}
/>

<CodeCard
  file="authz/ability.rs"
  note="one place for policy"
  lines={[
    { code: 'ab.can(Action::Read, user::Entity)' },
    { code: '    .when(|p| p.eq(user::OrgId, actor.org_id))  // your org only' },
    { code: '    .fields([user::Id, user::Name]);  // email never leaves', mark: true, marker: 3 },
  ]}
/>

<CodeCard
  terminal="two tokens, one route"
  lines={[
    { segs: [{ t: '❯', c: 'dim' }, { t: ' curl :3002/users -H ' }, { t: '"…Bearer $ADMIN"', c: 'str' }] },
    { segs: [{ t: '[{ "name": "Acme Admin", ' }, { t: '"email": "admin@acme.test"', c: 'bright' }, { t: ', … }]' }] },
    { segs: [] },
    { segs: [{ t: '❯', c: 'dim' }, { t: ' curl :3002/users -H ' }, { t: '"…Bearer $MEMBER"', c: 'str' }] },
    { segs: [{ t: '[{ "name": "Acme Admin" }]  ' }, { t: '# email masked, org-scoped', c: 'attr' }] },
    { segs: [] },
    { segs: [{ t: '❯', c: 'dim' }, { t: ' curl :3002/users/<globex-id> -H ' }, { t: '"…$MEMBER"', c: 'str' }] },
    { segs: [{ t: '403', c: 'err' }, { t: '  ' }, { t: '# cross-tenant read refused, by the framework', c: 'dim' }] },
  ]}
/>

</div>
</div>
</Band>

<Band label="02 — PERFORMANCE">
<div class="rs-band-head">

## How fast, exactly

<div class="rs-band-aside">

Same service, byte-identical contract, one pinned core each — the regime that
favours Node. Reproduce with `just bench`.

</div>

</div>

<Bench />

</Band>

<Band label="03 — BATTERIES" alt>

## Everything a backend needs

Not a router with plugins to assemble. Each concern is a module you import,
shipped as a Cargo feature you can leave off — a headless worker compiles no
HTTP stack.

<Capabilities />

<div class="rs-band-foot">

**31 capabilities**, one dependency to install them. [Packages →](/packages/)

</div>

</Band>

<Band label="04 — GUARANTEES">

## Why developers pick NestRS

<Guarantees />

</Band>

<Band label="05 — SCALABILITY" alt>

## One domain, several workloads

Entities, services and policy live once in a shared `features` crate; each app
under `apps/*` is a thin composition root importing only the transports it
serves. Microservice scaling without the distributed-systems tax.

<Workspace />

</Band>

<Band variant="rs-band--cta">

## Start where you are

Coming from NestJS, the vocabulary carries over. Coming from Rust, the
drift-prone parts stop being yours to police.

<div class="rs-cta-row not-content">
  <code><span class="rs-prompt">❯</span> nestrs new hello</code>
  <a class="rs-cta-button" href="/getting-started/">Get started</a>
</div>

</Band>
