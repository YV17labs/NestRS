---
title: Packages
description: Every opt-in crate the NestRS framework ships — grouped by concern, with Cargo names, umbrella features, and links to the docs.
---

import { Aside } from '@astrojs/starlight/components';

NestRS installs as **one dependency**. Each concern — HTTP, GraphQL, queues,
authorization, observability — is a **Cargo feature** you enable when the
binary needs it, and a feature you leave off is code you never compile. A
headless worker compiles no HTTP stack; an MCP-only assistant compiles no
GraphQL schema.

```toml title="Cargo.toml"
[dependencies]
nest-rs = { version = "3.0", features = ["http", "seaorm", "graphql"] }
```

Undecided? `features = ["full"]` turns everything on — narrow it later without
moving a single `use`. Either way `nest_rs::prelude::*` covers the decorators
and types an app reaches for daily.

The table below is a menu of **features**, not a list of crates to line up.

<Aside type="note">
This page lists **framework crates** — the batteries NestRS ships. Product
logic (users, posts, policies) lives in your workspace's `features` crate or
equivalent; see [The Publish workspace](/publish/) for how apps compose those
slices.
</Aside>

## Umbrella

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs) | *(always)* | Re-exports core + optional surface crates; `nest_rs::prelude` for the common case. One version to bump, and the **only** framework line your manifest needs — a decorator's expansion resolves through it | [Getting started](/getting-started/) |
| [`nest-rs-core`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-core) | *(via umbrella)* | DI container, `#[module]`, lifecycle hooks, boot-time access graph | [Fundamentals](/fundamentals/) |

Default features: `http`, `config` — every other surface crate is opt-in via
`features = [...]`.

## Transports

Each transport is a module you import in `AppModule`. HTTP-shaped surfaces
(GraphQL, OpenAPI, MCP, WebSocket gateways) mount on the same
[`HttpTransport`](/http/configuration/) — one port, one TLS config.

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-http`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-http) | `http` | HTTP transport — controllers, routes, extractors, built on poem | [HTTP](/http/) |
| [`nest-rs-graphql`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-graphql) | `graphql` | Resolver discovery, schema composition, async-graphql integration | [GraphQL](/graphql/) |
| [`nest-rs-ws`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-ws) | `ws` | WebSocket gateways — self-mount on the HTTP transport | [WebSockets](/websockets/) |
| [`nest-rs-openapi`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-openapi) | `openapi` | OpenAPI 3 document + bundled Swagger UI from the route table | [OpenAPI](/openapi/) |
| [`nest-rs-mcp`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-mcp) | `mcp` | Model Context Protocol server over HTTP | [MCP](/mcp/) |

## Data

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-database`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-database) | `database` | ORM-agnostic executor seam — ambient request/job context any driver implements | [Database](/database/) · [Writing a driver](/database/writing-a-driver/) |
| [`nest-rs-seaorm`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-seaorm) | `seaorm` | First-class SeaORM integration — `DatabaseModule`, `Repo`, transactions, dataloaders | [Database](/database/) |
| [`nest-rs-resource`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-resource) | `resource` | `#[expose]` — one entity declaration for GraphQL + OpenAPI + CRUD helpers | [CRUD](/database/crud/) · [Relations](/graphql/relations/) |
| [`nest-rs-storage`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-storage) | `storage` | S3-compatible object storage — injectable `Storage`, presigned URLs, via `object_store` | [Storage](/storage/) |

## Security

Authentication answers *who*; authorization answers *what they may do*.
Product-specific strategies and policies live in your `features` crate —
these crates are the engine.

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-authn`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-authn) | `authn` | JWT service, pluggable `Strategy`, `AuthnGuard` | [Authentication](/security/authentication/) |
| [`nest-rs-authz`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-authz) | `authz` | Ability-based authorization — access gate, row-level filter, response masking | [Authorization](/security/authorization/) |
| [`nest-rs-social`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-social) | `social` | Open `SocialProvider` contract — first-party GitHub/Google, third-party providers via the same seam | [Social login](/security/authentication/social-login/) |

## Request pipeline

Cross-cutting layers shared across HTTP, GraphQL, and WebSockets. Bind them
globally on `App::builder()` or per controller / resolver / gateway.

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-guards`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-guards) | `guards` | The `Guard` trait — gates access before the handler runs | [Guards](/fundamentals/guards/) |
| [`nest-rs-pipes`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-pipes) | `pipes` | Stateless input validation and transformation at the edge | [Pipes](/fundamentals/pipes/) |
| [`nest-rs-interceptors`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-interceptors) | `interceptors` | Wrap handler execution — logging, transactions, ability scope | [Interceptors](/fundamentals/interceptors/) |
| [`nest-rs-filters`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-filters) | `filters` | Map inner errors to transport responses | [Exception filters](/fundamentals/exception-filters/) |
| [`nest-rs-exception-filters`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-exception-filters) | `exception-filters` | Typed exception filters — catch one concrete error type | [Exception filters](/fundamentals/exception-filters/) |

## Background work

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-queue`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-queue) | `queue` | Backend-agnostic job contract + `#[process]` registry | [Queue](/queue/) |
| [`nest-rs-redis`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-redis) | `redis` | Redis-backed producer and worker (via apalis) | [Queue wiring](/queue/wiring/) |
| [`nest-rs-schedule`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-schedule) | `schedule` | Cron-style scheduled jobs — `#[scheduled]` + `ScheduleModule` | [Schedule](/schedule/) |
| [`nest-rs-events`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-events) | `events` | Typed in-process event bus — `#[listeners]` / `#[on_event]` | [Events](/events/) |
| [`nest-rs-worker`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-worker) | `worker` | Ambient job context seam for off-request transports | [Queue](/queue/) · [Writing a driver](/database/writing-a-driver/) |

## Platform

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-config`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-config) | `config` | Namespaced env + file config bound to typed `#[config]` structs | [Configuration](/configuration/) |
| [`nest-rs-health`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-health) | `health` | Kubernetes-style liveness / readiness / startup probes | [Health](/health/) |
| [`nest-rs-throttler`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-throttler) | `throttler` | Per-route rate limiting via `ThrottlerGuard` | [Throttler](/rate-limiting/) |
| [`nest-rs-opentelemetry`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-opentelemetry) | `opentelemetry` | Logs, traces, metrics, W3C propagation, OTLP export | [OpenTelemetry](/opentelemetry/) |
| [`nest-rs-server-timing`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-server-timing) | `server-timing` | W3C `Server-Timing` response header (independent of OTel) | [Server-Timing](/server-timing/) |

## Developer tools

| Crate | Cargo feature | What it is | Docs |
|---|---|---|---|
| [`nest-rs-cli`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-cli) | *(binary: `nestrs`)* | Scaffold workspaces, generate features, project health checks | [CLI](/cli/) |
| [`nest-rs-testing`](https://github.com/YV17labs/NestRS/tree/main/crates/nest-rs-testing) | `testing` | In-process harness — boot the real DI graph, drive HTTP/GraphQL/MCP without a socket | [Testing](/testing/) |

## Typical binary profiles

These are **composition patterns**, not separate crates — they show which
packages a deployable usually pulls in.

| Binary role | Packages you typically import |
|---|---|
| REST + OpenAPI API | `http`, `openapi`, `config`, `seaorm`, `authn`, `authz`, `health` |
| GraphQL API | `http`, `graphql`, `config`, `seaorm`, `authn`, `authz` |
| WebSocket live service | `http`, `ws`, `config`, `seaorm`, `authn`, `authz` |
| Queue worker | `queue`, `redis`, `schedule`, `seaorm`, `config` |
| MCP assistant | `http`, `mcp`, `config`, `authn`, `authz` |
| Token issuer | `http`, `config`, `seaorm`, `authn` |

See [The Publish workspace](/publish/) for five real apps that follow these
profiles.

## Not listed here

**Internal macro crates** (`nest-rs-*-macros`, `nest-rs-codegen`) implement
the decorators — you never add them as direct dependencies; the surface crate
re-exports what you need. For a decorator index see
[Decorators](/decorators/).

**Your product's `features` crate** holds domain modules (entities, services,
controllers per transport). It is workspace-local and not published to
crates.io — copy [`crates/features/src/users/`](https://github.com/YV17labs/NestRS/tree/main/demo/crates/features/src/users/)
as the exemplar.

## Adding a capability to your app

Turn on its **feature**. There is one framework line, and adding a transport
never adds a second — this is the real `apps/api/Cargo.toml` (REST + GraphQL +
DB + authz), trimmed to the framework line:

```toml title="apps/api/Cargo.toml"
[dependencies]
nest-rs = { workspace = true, features = [
    "http", "config", "seaorm", "graphql", "openapi", "authn", "authz", "health",
] }
```

In a fresh project the same step is
`cargo add nest-rs --features <capability>` — exactly what each module's
adoption page shows. Then import the matching **modules** in `AppModule` — for
example `HttpModule::for_root(...)`, `DatabaseModule::for_root(...)`,
`GraphqlModule`, `AuthzHttpModule`. The [Publish workspace](/publish/) and
[Getting started](/getting-started/) walk through a full composition.

### Why one entry is enough

A decorator is a procedural macro, and its expansion resolves paths against
**your** crate's extern prelude. NestRS decorators root theirs at
`::nest_rs::<concern>::…`, which that single entry reaches — so the feature list
*is* the install. Nothing a decorator expands into is a line you write.

Get the *dependency* wrong and the compiler says so in your own terms:

```text frame="terminal"
error: this nestrs decorator expands into the framework, which this crate
       cannot reach. Add to Cargo.toml:

           nest-rs = { version = "3.0", features = ["http"] }
```

Get the **feature** wrong — the likelier mistake, since the dependency is
already there — and the diagnostic is rustc's own. A missing feature reads as a
missing item, with a `note` telling you which:

```text frame="terminal"
error[E0433]: failed to resolve: could not find `seaorm` in `nest_rs`
   |
   = note: found an item that was configured out
   = note: the item is gated behind the `seaorm` feature
```

Add the feature named in the note to the `nest-rs` line — never a second crate.

The `nest-rs-*` crates stay published and keep their names — they are separate
compilation units, which is what makes an unused capability free. They are not
the entry point, and renaming the `nest-rs` dependency is unsupported: the same
limit `tokio` has, for the same reason.

### What stays in your manifest

Crates **your own code names**. A job payload deriving `serde::Serialize`, an
indicator returning `anyhow::Result`, an entity written against `sea-orm` — those
are yours, and hiding them behind a re-export would cost you their feature flags
and a truthful `cargo tree`. No Rust framework hides them; NestRS does not
either.

**GraphQL** used to be the one exception, and no longer is: `#[resolver]` wraps
async-graphql's `#[Object]`, but it now pins that expansion to the framework's
own re-export, so `nest-rs --features graphql` alone compiles a resolver.
`async-graphql` returns to the rule above — yours when your code names it,
which for its *types* you can avoid entirely with
`use nest_rs::graphql::async_graphql::{Context, Result};`, and for its *derives*
you cannot, exactly as for `serde`.

## Going further

- [Getting started](/getting-started/) — scaffold an app and write the first route.
- [The Publish workspace](/publish/) — five apps that follow these binary profiles.
- [Fundamentals](/fundamentals/) — the DI container every capability wires into.
