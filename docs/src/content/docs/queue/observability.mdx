---
title: Observability
description: The span and events the queue worker emits per job — what you see at debug, info, and warn levels, and the boot lines that precede them.
tier: all-options
sidebar:
  order: 5
---

import { Aside } from '@astrojs/starlight/components';

Every queue event the framework emits targets `nest_rs::queue`. One
target, every worker-side log filterable by `RUST_LOG=nest_rs::queue=debug`,
one span wrapping each job so its events share the same fields. The
service-level logs your `#[process]` method emits keep their own target
(e.g. `features::audio`) — same convention as the rest of the framework.

## Targets and levels

| Target | What | Level |
|---|---|---|
| `nest_rs::queue` | Boot registration, per-job span + events, skipped/unversioned warnings. | `debug` on job start, `info` on success, `warn` on failure. |
| `nest_rs::app` | Boot: `attached module-contributed transport transport="QueueWorker"`. Emitted by the composition root, which attaches every transport — so the target is the app's, not the queue's. | `info`. |
| `features::<feature>` | Your service-level logs inside the `#[process]` method. | Your call. |

Hot paths respect `RUST_LOG=info`: `job started` sits at `debug`, so a
worker at `info` logs one `job ok` line per successful ack and a
`job failed` line per failed attempt — nothing per-poll noise.

## The per-job span

The worker opens one span per job attempt, target `nest_rs::queue`,
name `process job`. The handler runs inside it, so any `tracing` event
your service emits — and the framework's own start/end events — are
tagged with the span's fields:

| Field | Notes |
|---|---|
| `queue` | The Redis queue name. |
| `processor` | The processor host name, e.g. `AudioProcessor::transcode`. |
| `job_id` | The apalis `TaskId` for this job. |
| `attempt` | The apalis attempt counter (`1` on the first try, higher on retries). |
| `trace_id` / `span_id` | This job's [W3C trace context](/fundamentals/correlation/) — always present. |
| `parent_span_id` | The span that enqueued the job: the HTTP request, the message, the tick. |
| `continued_trace` | `true` when the envelope carried a `traceparent`, `false` when this job started a trace of its own. |
| `actor_id` | Who the *enqueue* was served for. Absent when nobody was authenticated — a job pushed by a scheduled tick has none. |
| `otel.kind` | `consumer` — a job is work delivered *to* this process. |

`attempt` distinguishes retries of the same `job_id`. The OTLP appender
from `nest-rs-opentelemetry` exports these as span attributes.

The trace fields cross a **process** boundary: the producer seals them
into the job envelope and the consumer continues from them — see
[Correlation](/fundamentals/correlation/).

## What a job's life looks like

On success the worker emits `job started` at `debug` and `job ok` at
`info` (carrying `elapsed_ms`, the wall time of the handler call):

```text frame="terminal"
DEBUG process job{otel.kind="consumer" trace_id=01a014a851807b2196a2b35038bd442b span_id=d0c61da1201df4a8 queue="audio" processor="AudioProcessor::transcode" job_id=01HFEX… attempt=1 continued_trace=true parent_span_id=fdbb09d1841e9ec8 actor_id="00000000-0000-7000-8000-00000000ac00"}: nest_rs::queue: job started
 INFO process job{otel.kind="consumer" trace_id=01a014a851807b2196a2b35038bd442b span_id=d0c61da1201df4a8 …}: features::audio: transcoded file="track-1717405126521.mp3"
 INFO process job{otel.kind="consumer" trace_id=01a014a851807b2196a2b35038bd442b span_id=d0c61da1201df4a8 …}: nest_rs::queue: job ok elapsed_ms=342
```

A failed attempt emits `job failed` at `warn`, carrying `elapsed_ms`
and the `error`:

```text frame="terminal"
DEBUG process job{… job_id=01HFFA… attempt=1}: nest_rs::queue: job started
 WARN process job{… job_id=01HFFA… attempt=1}: nest_rs::queue: job failed elapsed_ms=12 error=connection refused
```

The `attempt` field on the span is how you correlate a retry: apalis
re-runs the same `job_id` with `attempt=2`, opening a fresh `process
job` span — same `trace_id`, new `span_id`. There is no separate
terminal line — the retry budget lives in the apalis `RetryLayer`; see
[Retries and failure](/queue/retries-and-failure/) for the policy behind
`attempt`.

## What boot looks like

Each active processor logs one `attached` line (from the transport) and
one `registered queue processor` line carrying its `retries` budget. A `#[process]` method that compiled in but whose
provider isn't reachable from this app's module tree is skipped with a
`warn`:

```text frame="terminal"
 INFO nest_rs::app: attached module-contributed transport transport="QueueWorker"
 INFO nest_rs::queue: registered queue processor processor="AudioProcessor::transcode" queue="audio" retries=3
 WARN nest_rs::queue: skipped #[process] method: no instance of the provider in
      this app's container processor=ReportJobs::weekly queue=reports
      origin="features::reports::queue::processor"
      hint="nothing is registered under that exact type. Common causes: its module
      is not imported by this app (import it, or delete the methods); it is bound
      only as `dyn Trait`; it is imported through a `for_root`; it is registered by
      hand under a key or a trait"
```

The skip line is `warn` — a linked-but-unimported handler is usually a
wiring mistake worth surfacing, and it confirms a shared `features`
crate isn't accidentally activating handlers this binary doesn't want.
See [Wiring the worker](/queue/wiring/) for the module-gating rule.

<Aside type="caution">
A legacy payload with no wire envelope logs `processed an unversioned
job payload` at `warn` (fields `queue`, `hint`) before it runs. It
still executes; drain the queue to clear pre-envelope jobs. A payload
tagged with an *unsupported* version fails the job instead of warning.
</Aside>

## Filtering recipes

```bash
RUST_LOG=info,nest_rs::queue=debug nestrs run dev worker
```

`info` everywhere, `debug` on the queue path: adds the `job started`
line per poll on top of the `job ok` / `job failed` outcomes.

```bash
RUST_LOG=nest_rs::queue=warn nestrs run dev worker
```

Strictly failures, skips, and unversioned warnings — useful for tailing
a healthy worker where the only thing you care about is "did anything go
wrong."

## Going further

- [OpenTelemetry](/opentelemetry/) — installing the OTLP appender so
  these spans and structured logs reach a backend.
- [OpenTelemetry / traces](/opentelemetry/traces/) — how
  `nest_rs::queue` spans appear in a trace UI, attribute conventions.
- [OpenTelemetry / logs](/opentelemetry/logs/) — structured-log
  export, severity mapping.
- [Retries and failure](/queue/retries-and-failure/) — the policy
  behind the `attempt` field and the `failed` outcome.
